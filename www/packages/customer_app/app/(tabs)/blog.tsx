import React from "react";
import { Text, View } from "react-native";

/**
 * Blog tab: one of the 5 primary tabs in customer home screen.
 * Displays a list of informative articles/blogs (e.g., tips and tricks)
 * aimed at providing value to the user.
 */
export default function Blog() {
  return (
    <View>
      <Text>Tab: blog</Text>
    </View>
  );
}
