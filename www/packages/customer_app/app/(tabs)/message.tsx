import React from "react";
import { Text, View } from "react-native";

/**
 * Message Tab: one of the 5 primary tabs in customer home screen.
 * This screen serves as the **communication center** where customers can:
 * 1. View a list of all conversations with connected handymen (contacts).
 * 2. Initiate or continue real-time, one-on-one chats with handymen.
 */
export default function Message() {
  return (
    <View>
      <Text>Tab: Message</Text>
    </View>
  );
}
